# ConfigMap - konfiguracja (nie-wrażliwe dane)
# Łatwo zmienić bez przebudowy obrazu
apiVersion: v1
kind: ConfigMap
metadata:
  name: translator-config
  namespace: translator
data:
  NODE_ENV: production
  PORT: "4000"
  LOG_LEVEL: info
  CORS_ORIGIN: http://localhost:3000
